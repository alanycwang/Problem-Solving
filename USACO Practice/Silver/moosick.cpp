#include <iostream>

using namespace std;

int main(){
  int n, c;
  cin >> n;
  int song[n];
  for (int i = 0; i < n; i++){
    cin >> song[i];
  }
  cin >> c;
  int chord[c];
  for (int i = 0; i < c; i++){
    cin >> chord[i];
  }
  int modifier;
  for (int i = 0; i < n-c; i++){
    array x[c];
    for (int j = 0; j < c; j++){
      x[j] = song[j+i];
    }
    for (int j = 0; j < c; j++){
      for (int k = 0; k < c; k++){
        for (int l = 0; l < )
      }
    }
  }
}
