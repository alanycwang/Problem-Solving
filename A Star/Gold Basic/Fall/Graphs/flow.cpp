#include <iostream>

using namespace std;

struct pipe{
  char start, end;
  int capacity;
};

int main(){
  int n;
  cin >> n;
  pipe pipes[n];
  for (int i = 0; i < n; i++{
    cin << pipes[i].start << pipes[i].end << pipes[i].capacity;
  }
  for (int i = 0; i < n; i++){

  }
}
