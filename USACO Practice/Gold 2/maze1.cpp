#include <iostream>
#include <string>
#include <queue>

using namespace std;

struct location {
  int x, y;
}

int main(){
  int w, h;
  cin >> w >> j;
  string map[h];
  for (int i = 0; i < h; i++){
    cin >> map[i];
  }
  queue
  for (int i = 1; i < (w-1)/2; i+=2){
    if (map[0][i] == ' '){

    }
  }
}
