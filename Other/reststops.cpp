#include <iostream>

using namespace std;

int main(){
  /*freopen("reststops.in", "r", stdin);
  freopen("reststops.out", "w", stdout);*/
  int l, n, rf, rb;
  cin >> l >> n >> rf >> rb;
  int stops[n][2];
  for (int i = 0; i < n; i++){
    cin >> stops[i][0] >> stops[i][1];
  }
  int times[n];
  for (int i = 0; i < n; i++){
    times[i] = rf
  }
}
